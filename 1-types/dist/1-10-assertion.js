{
    /**
     * Type Assertions 💩
     */
    function jsStrFunc() {
        return 2;
    }
    var result = jsStrFunc();
    console.log(result.length);
    console.log(result.length);
    var wrong = 5;
    console.log(wrong.push(1)); // 😱
    function findNumbers() {
        return undefined;
    }
    var numbers = findNumbers();
    numbers.push(2); // 😱
    var button = document.querySelector('class');
    var num = {
        age: 24
    };
}
