{
    // Array
    var fruits = ['🍅', '🍌'];
    var scroes = [1, 3, 4];
    function printArray(fruits) { }
    // Tuple -> interface, type alias, class
    var student = void 0;
    student = ['name', 123];
    student[0]; // name
    student[1]; // 123
    var name = student[0], age = student[1];
}
